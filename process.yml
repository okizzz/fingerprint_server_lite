apps:
  - script   : ./index.js
    name     : 'server'
    instances : 'max'
    exec_mode : 'cluster'
    max_memory_restart: '350M'
    listen_timeout: 3000
    restart_delay: 0
    autorestart: true
    watch: true
